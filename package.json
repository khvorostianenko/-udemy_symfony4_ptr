{
  "devDependencies": {
    "@symfony/webpack-encore": "^0.22.2"
  },
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev-server": "encode dev-server",
    "dev": "encore dev",
    "watch": "encore dev --watch",
    "build": "encore production"
  }
}
